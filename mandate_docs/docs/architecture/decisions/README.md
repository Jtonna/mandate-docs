# Architecture decision records

These records capture the design decisions behind the mandate parser,
validator and run command, each as one choice with its context and
consequences. Files are numbered `NNNN-short-slug.md`, starting at `0001`;
a new decision gets the next unused number.
